# 2026-07-02 mfs.xx.kg 修复说明 + 经验

## 摘要

7/2 15:43 完成 mfs.xx.kg 18 篇文章 FAQ 尾部 latin-1 二次编码修复，100% 通过。

| 项 | 数据 |
|---|---|
| 修前 BAD 文章 | 18 篇 (拉丁1 mojibake 标记 ≥25 处/篇) |
| 修后 全部 | 0 篇 BAD |
| 验证 | 重扫 37 篇 mjCount ≤5 |
| 路径 | chrome CDP eval fetch + WP REST POST |

## 根因

之前 6/26 用 PATCH 注入 mfs-faq-block 时，UTF-8 字节被当 latin-1 字符串读出再 UTF-8 encode，
丢失字节不完整，导致 FAQ 中文碎成 latin-1 字符（`å¸¸è§ é®é¢` 看不出"常见问题"）。

## 修复办法

每篇文章 POST `/wp-json/wp/v2/posts/{pid}`，content 字段：
1. Python regex `FAQ_BLOCK_RE = re.compile(r'\n?\s*<div\s+class="mfs-faq-block"[^>]*>.*?</div>\s*\n?', re.DOTALL)`
2. replace 子段为新规范化 FAQ 块（3 个 FAQ QA + wx CTA）
3. WP REST 写入成功（HTTP 200）

## FAQ 内容（统一替换）

```html
<h2>常见问题</h2>
<details>
  <summary>跨境业务为什么要用静态住宅 IP？</summary>
  <p>机房 IP / 云厂商 IP 长期被打标签，跨境平台（亚马逊、TikTok、PayPal）风控看到就自动降权；ISP 静态住宅 IP 在 Scamalytics 等公开库分数通常<30，平台不容易直接风控。同一 IP 长期独占 + ISP 属性 = 安全度最高。</p>
</details>
<details>
  <summary>怎么检测一个 IP 是不是真住宅？</summary>
  <p>查 IP 的 WHOIS 信息看 owner.type 是否 residential，或用 ipinfo.io / ipapi.co 看 type 字段；如果显示 isp=数据中心/cloud/hosting，肯定不是真住宅。平台风控也查 ASN 属性，139.com / 阿里云 / aws 这种 ASN 必中招。</p>
</details>
<details>
  <summary>一台机器能挂几个住宅 IP？</summary>
  <p>技术上无限，但实际看场景。亚马逊 / TikTok 店铺这类多账号业务建议 1 IP = 1 店；批量数据采集按目标站反爬强度决定，通常单个 IP QPS 控制在 3-5 以内才稳。具体策略可以聊，不建议硬套模板。</p>
</details>
+ wx CTA: yisheng3wantian
```

## 关键脚本

`C:\Users\xiao\fix_faq_blocks.py` (3612 B, 可重复使用)
`C:\Users\xiao\scan_all_v2.py` (扫描器)
`C:\Users\xiao\verify_fix.py` (验证脚本)

## WP REST 注入链 教训

1. **不要**用 `str.encode('latin-1').decode('utf-8')` 反向解码丢失字节
2. **不要**让 puppeteer + json.dumps(ensure_ascii=True) 自动二次转义
3. **要**先 chrome page eval 内 fetch 看 raw，写 Python 内存里 regex replace 后，再 PATCH
4. **要**验证：rawLen → repairLen，re-render cjk count 增加 + mj count 减少

## 状态

- mfs.xx.kg 后端: ✅ 修完
- 搜索引擎收录: 已 IndexNow 重推 70 URL + 百度 push 10/10 已用完
- 测速新数字: 7/3 重扫 mojibake = 0

## 仍然不可解的边界

- **头条** bytedance axe anti-bot 服务端束拦截 — 草稿还在 mp.toutiao.com/profile_v4/manage/draft，需老板手动扫码发布
- **FYI**: 超过 6/27 升级后 wp-login.php 也被 WAF AES 跳墙, **唯一登 mfs 后台路径**：
  1. PowerShell `Start-Process chrome --remote-debugging-port=9225 --user-data-dir=chrome-cdp-ps1`
  2. 在已登态 Chrome 内 fetch wp-json 拿 nonce
  3. PATCH POST
