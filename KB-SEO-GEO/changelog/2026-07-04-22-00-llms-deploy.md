## 2026-07-04 22:00 · mfs.xx.kg llms.txt + GEO 全套部署（深夜 boss session）

### 主要动作
- ✅ **mfs.xx.kg llms 入口链路建立**：
  - 创建 WP Page 678 (slug: llms-txt, title: llms.txt)，内容含 36 篇 publish 文章按 8 业务分类
  - FTP 上传 llms.php (17702B) + llms.txt (17469B) + uploads/llms.txt + llms.html — 物理全布点
  - 模板 `template-llms-raw.php` 创建在 twentytwentyfive 主题（待 block theme routing 配套）
- ✅ **WP Page 内容真实入库**：通过 chrome CDP + WP REST (X-WP-Nonce 路径) 创建页
- ⚠️ **.htaccess IFE 锁死**：FTP 不能改 .htaccess，物理 .txt / .php / .html 在根都被 LiteSpeed catch-all 吃 404
- ⚠️ **robots.txt 是 openresty 静态输出**，WP REST 改不动（需要 IFE 控制面板/工单）

### mfs 站可达状态（chrome 真客户端视角）
- `/llms-txt/` → 200, content_length=203150B, 含完整 markdown 36 篇文章 + yisheng CTA + GEO + TikTok
- AI bot (GPTBot / ClaudeBot / 字节跳动 / 百度文心) — 真客户端视角应同样能看到

### 关键发现（永久）
- mfs.xx.kg HTTPS curl 全走 WAF (HTTP 200 SIZE=0 / 404) 但 chrome + REST GET 正常 — WAF 只对未知 UA 拦截
- V2 + python requests + verify=False + 5 件套 header —— 唯一稳定拉 mfs 数据的方式
- IFE 不允许普通 FTP 账号动 .htaccess + robots.txt — 是 by design
- llmstxt.org 标准不强制 raw markdown：HTML 包 markdown 也被 AI bot 接受作为 llms 实体的入口
- 物理 .html / .txt 在 mfs 站根 100% 会被 LiteSpeed catch-all rewrite — 即使物理存在

### 后续行动（boss 拍板项）
1. 老板 IFE 控制面板手动加 `OAI-SearchBot` + `Claude-SearchBot` Allow 段 + `Sitemap: /llms-txt/`
2. 等下一次 IFE 控制面板可写 robots.txt 时统一刷新
3. 5 引擎 query 验证 llms-txt 链接被引用（等老板登 5 引擎后小柔接管）
4. 月跑 llms.txt 重生成（每发新文章后）

### 数字
- 36 文章分类: GEO 4 / Reddit 等 2 / TikTok 4 / 电商 4 / AI 大模型 4 / IP 选型 5 / IP 采购 5 / 通用 8
- llms.txt: 17469 bytes (本地) / 17702 bytes (llms.php 包装)
- FTP LIST: 28 文件在 htdocs/
